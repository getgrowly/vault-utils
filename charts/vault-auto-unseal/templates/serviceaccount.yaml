{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vault-auto-unseal.serviceAccountName" . }}
  namespace: {{ .Values.vault.namespace }}
  labels:
    {{- include "vault-auto-unseal.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
