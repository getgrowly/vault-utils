apiVersion: v1
kind: Service
metadata:
  name: {{ include "vault-auto-unseal.fullname" . }}
  labels:
    {{- include "vault-auto-unseal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vault-auto-unseal.selectorLabels" . | nindent 4 }}
